import http from 'node:http';import path from 'node:path';import {readFile} from 'node:fs/promises';import {fileURLToPath} from 'node:url';
const root=path.dirname(fileURLToPath(import.meta.url));const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.png':'image/png'};
http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://localhost');const f=['/','/quality-incident-console','/quality-incident-console/'].includes(u.pathname)?'index.html':decodeURIComponent(u.pathname).slice(1);const t=path.resolve(root,f);if(!t.startsWith(root+path.sep))throw Error();const b=await readFile(t);res.writeHead(200,{'Content-Type':mime[path.extname(t)]||'application/octet-stream','Cache-Control':'no-cache'});res.end(b)}catch{res.writeHead(404).end('Not found')}}).listen(5190,'127.0.0.1',()=>console.log('http://localhost:5190/quality-incident-console'));
