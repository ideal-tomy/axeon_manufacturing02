import {state,go} from './state.js';import {Hero,Dashboard,Incident,RootCause,Corrective} from './views.js';
const app=document.querySelector('#app');const labels=['ダッシュボード','不具合一覧','AI分析','原因調査','是正・承認'];
function shell(content){return `<header class="topbar"><button class="logo" data-action="hero"><span class="logo-mark">QI</span><span><strong>Quality Incident Console</strong><small>QUALITY RESPONSE DEMO</small></span></button><div class="top-meta"><span>サンプル工場</span><span class="demo-pill">DEMO DATA</span></div></header><div class="shell"><aside class="sidebar"><nav>${labels.map((x,i)=>`<button class="side-item ${state.page===i+1?'active':''}" data-action="page:${i+1}"><span class="side-icon">${['▦','▣','◌','⌁','✓'][i]}</span>${x}</button>`).join('')}</nav><div class="side-foot"><span class="sample-dot"></span><span>東海精密工業<small>架空データ</small></span></div></aside><main class="console-main">${content}</main></div>`}
function render(){app.innerHTML=state.page===0?`<div class="hero-shell">${Hero()}</div>`:shell([null,Dashboard,Incident,RootCause,Corrective][state.page]());}
app.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(!b||b.dataset.action==='none')return;const a=b.dataset.action;if(a==='hero')go(0);else if(a==='dashboard')go(1);else if(a==='incident')go(2);else if(a==='root-cause')go(3);else if(a==='corrective')go(4);else if(a.startsWith('page:'))go(Number(a.slice(5)));else if(a==='ai')go(2);else if(a.startsWith('cause:'))state.selectedCause=a.slice(6);else if(a==='restart')go(0);render();});render();
